// Responsive breakpoints

export const smallScreenSize = () => window.innerWidth <= 650;

export const mediumScreenSize = () =>
  window.innerWidth >= 651 && window.innerWidth <= 1999;

export const largeScreenSize = () => window.innerWidth >= 1200;
